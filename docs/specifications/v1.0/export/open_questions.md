# Preguntas abiertas — Export Specifications v1.0

> Agrupadas según lo pedido: globales, por módulo, por objeto, Action
> Plans, templates Enablon, formato CSV, configuración por cliente. Cada
> pregunta cita la evidencia que falta para cerrarla. Ninguna de estas
> preguntas bloquea el trabajo de otro objeto salvo que se indique
> explícitamente.
>
> **Actualización — incremento de mapping evidence (Bloque2)**: se abrieron
> los 9 ZIP de `Bloque2_Mappings_SQL_ENA/` — ver
> [`mapping_evidence_assessment.md`](mapping_evidence_assessment.md).
> `OQ-GLOBAL-04` y `OQ-AP-03` quedaron **resueltas** (los ZIP se abrieron; el
> resultado fue negativo, no positivo). Se añadió `OQ-GLOBAL-06` (anomalía
> de ZIP anidado).
>
> **Actualización — incremento de evidencia ETL/entidad (Bloque1 y
> Bloque3)**: se abrieron y analizaron los 10 workbooks ETL y los 2
> workbooks de mapeo de entidad — ver
> [`etl_evidence_assessment.md`](etl_evidence_assessment.md) y
> [`entity_mapping_assessment.md`](entity_mapping_assessment.md).
> `OQ-GLOBAL-04` queda ahora **totalmente resuelta** (el insumo de mapping
> de campo, antes sin abrir, ya se abrió y sí contenía mapping real, a
> diferencia de Bloque2). `OQ-GLOBAL-02` amplía su alcance (titlefix
> aparece también en Simulacros y Bypass, no solo MOC/GCT).
> `OQ-OBJ-03` (Impacts) avanza a `pending_confirmation` con nueva
> evidencia parcial. Se añaden `OQ-ETL-01..04` (Drills) y `OQ-ENT-01..03`
> (mapeo de entidad) y `OQ-OBJ-07` (vocabulario de estado distinto entre
> Drills y List of Activities).
>
> **Actualización — incremento de validación de Reference / análisis
> profundo ETL / Action Plans cuantitativo**: `OQ-ETL-03` queda
> **`resolved`** (regla de `Reference` aprobada y registrada como
> `AFD-DRILLS-REFERENCE-001` — ver
> [`decision_packages/drills_reference_decision_package.md`](decision_packages/drills_reference_decision_package.md)),
> sin eliminar su histórico. `OQ-ETL-04` queda **`resolved`** (resolución
> de entidad de Drills localizada — ver
> [`object_assessments/drills_entity_resolution_assessment.md`](object_assessments/drills_entity_resolution_assessment.md)).
> `OQ-AP-02` avanza con cuantificación real sobre el CSV completo (ver
> [`action_plan_relationship_analysis.md`](action_plan_relationship_analysis.md)),
> sin cerrarse. Se añaden `OQ-ETL-05`, `OQ-ETL-06` (nuevas, no reabren
> `OQ-ETL-03`), `OQ-ENT-04`, `OQ-AP-04`, `OQ-AP-05`, `OQ-AP-06`. Se
> actualiza la hipótesis de `OQ-OBJ-04` (Causes Data → candidato más fuerte:
> PSM Forms, no Investigations).

## Globales

| question_id | Prioridad | Objeto relacionado | Evidencia faltante | Impacto | Responsable sugerido | Estado |
|---|---|---|---|---|---|---|
| OQ-GLOBAL-01 | Alta | `eventos.PSM_Forms` | Confirmar si `sql/source_queries/PSM/` (folder propio) y el submódulo `psm` de `config/modules.yaml` (bajo `eventos`) describen la misma fuente, o si son dos organizaciones históricas distintas nunca reconciliadas. Abrir `PSM.zip` (Bloque2) no aportó evidencia nueva — solo confirma un duplicado de las mismas 2 queries, sin resolver la ambigüedad estructural. | Sin resolver, cualquier `ExportDefinition` de PSM Forms puede construirse sobre una fuente SQL equivocada. | Equipo de análisis + cliente (confirmar tabla origen real) | `pending_confirmation` |
| OQ-GLOBAL-02 | Media | `moc.Change_Register`, `simulacros.Drills`/`List_of_Activities`, `bypass.By_Passes` | Quién/qué ejecuta `titlefix` en la práctica — confirmado que strip de comillas ocurre en el dato real (MOC), VBA del libro MOC confirmado 100% vacío. **Ampliado en el incremento de evidencia ETL**: la hoja `CharacterFix`/`Mapeo_Titulo` con la misma regla `titlefix` existe también en los workbooks de Simulacros y Bypass (sistema ITP, no solo GCT) — la pregunta ya no es "¿qué ejecuta titlefix en MOC?" sino "¿qué ejecuta titlefix en cualquier ETL que la declare?", dado que ningún workbook auditado (ITP o GCT) tiene el motor visible. | Sin esto, no se puede documentar la regla como `ReglaEspecial` reproducible en un `ExportDefinition`, para NINGÚN módulo que la use. | Cliente (proceso externo desconocido) | `evidence_pending` — alcance ampliado, sin avance en la respuesta |
| OQ-GLOBAL-03 | Media | Todos | Reconciliar el naming `ap` (config) vs `action_plans` (código/tests) y `safety_meetings` (config) vs `reuniones_de_grupo` (tests, y el nombre del ETL `reunionesdegrupo`). Dato adicional de este incremento: ni `Reunionesdegrupo.zip` ni `sql/source_queries/SM/` usan `reuniones_de_grupo` internamente — ambos usan el slug `SM`. El identificador `reuniones_de_grupo` parece existir únicamente en `tests/` y en el nombre de archivo del ETL/ZIP, nunca en el contenido SQL. | Riesgo de que una futura especificación use una clave distinta a la que ya usa el código, rompiendo trazabilidad. | Equipo de desarrollo (decisión de convención única) | `pending_confirmation` |
| OQ-GLOBAL-04 | Baja | Todos | **Totalmente resuelta.** Los 9 zips de Bloque2 no contenían mapping de campo (incremento de mapping evidence). Los 10 workbooks de `Bloque1_ETL/` y los 2 de `Bloque3_Mappings_Entidades/` sí se abrieron en el incremento de evidencia ETL/entidad y SÍ contienen mapping de campo/entidad real, confirmado con fórmula (ver `etl_evidence_assessment.md`, `entity_mapping_assessment.md`). Restante: solo Simulacros se analizó fila a fila; los otros 9 ETL solo vía su hoja Index — sus hojas de mapeo internas siguen sin leerse en detalle (no es ya una carencia de "ZIP sin abrir", es alcance de profundidad de lectura). | Ya no bloquea nada — el mapping de campo real ya se localizó y se confirmó su forma. | Equipo de análisis (profundizar en los 9 ETL restantes en un futuro incremento) | `resolved` |
| OQ-GLOBAL-05 | Baja | Todos | 727 de 834 tickets de Help Desk siguen sin categorizar por módulo. | Puede haber defectos relevantes para el diseño de exportación aún no cruzados contra los hallazgos técnicos. | Equipo de análisis | `evidence_pending` |
| OQ-GLOBAL-06 | Media | `simulacros.Drills`, `safety_meetings.Group_Meetings` | `Simulacros.zip` contiene una entrada anidada `SM.zip` que es, byte a byte, una copia íntegra de `Reunionesdegrupo.zip`. `Simulacros.zip` tiene fecha de modificación un día posterior a los otros 8 ZIP. No se ha determinado si fue un empaquetado intencional (¿por qué incluir Safety Meetings dentro de Simulacros?) o un error de quien preparó el envío. | Si es intencional, podría indicar una relación entre ambos módulos no documentada en ningún otro lugar; si es accidental, es solo ruido de packaging sin significado funcional — no se puede saber cuál sin preguntar. | Cliente (o quien preparó el paquete `_incoming_claude_web`) | `evidence_pending` |

## Por módulo

| question_id | Prioridad | Objeto relacionado | Evidencia faltante | Impacto | Responsable sugerido | Estado |
|---|---|---|---|---|---|---|
| OQ-MOD-01 | Alta | `moc` (módulo) | Confirmación de CCP (`config/modules.yaml:118`: "pendiente confirmar CCP") — sin desarrollar en ningún documento disponible qué es "CCP" en este contexto. | Bloquea cerrar el estado `abierto` de MOC independientemente de exportación. | Cliente | `evidence_pending` |
| OQ-MOD-02 | Alta | `moc` (módulo) | Resolución del Hallazgo #1 en MOC (gap -39%, La Rábida/Palos) — confirmado pero no explicado causalmente. | Cualquier CSV de Change Register heredaría el mismo sesgo de infra-representación si se genera antes de resolver esto. | Cliente + equipo de análisis | `pending_confirmation` |
| OQ-MOD-03 | Media | `ap` (módulo) | `visitas_seguridad_y_otros` no tiene ningún SQL, ETL o CSV propio identificado — solo existe como `idorigenac [7,14]` anidado. | No bloquea otros módulos (Tarea 6), pero cualquier Action Plan clasificado bajo esos códigos queda sin trazabilidad de origen. | Cliente (confirmar si este módulo existe como entidad real en Enablon) | `evidence_pending` |
| OQ-MOD-04 | Media | `ops` (módulo) | `ops2` no tiene ningún dato de enlace hacia Action Plans — "gap reconocido explícitamente por el propio ETL" (`config/modules.yaml:276`), sin explicación adicional. | Acciones del sistema OPS nuevo pueden quedar huérfanas de su Action Plan correspondiente. | Cliente | `evidence_pending` |
| OQ-MOD-05 | Baja | `simulacros`, `safety_meetings`, `bypass`, `ops`, `ap` (módulos) | `hallazgo_1_infra_migracion.modulos_pendientes_de_revisar` (`config/modules.yaml:71`) — estos 5 módulos aún no se han desglosado por site para confirmar o descartar el mismo patrón visto en Eventos/Inspecciones/MOC. | Cualquier volumetría agregada de estos módulos puede esconder el mismo patrón, por indicación explícita de `CLAUDE.md` Hallazgo #1. | Equipo de análisis | `pending_confirmation` |

## Por objeto

| question_id | Prioridad | Objeto relacionado | Evidencia faltante | Impacto | Responsable sugerido | Estado |
|---|---|---|---|---|---|---|
| OQ-OBJ-01 | Alta | `simulacros.List_of_Activities` | No aparece en absoluto en `config/modules.yaml` — solo se conoce por ADR-001 (mención) y por su CSV real. Sin fuente SQL, clave de correlación ni relación documentadas a nivel de objeto. | Sin esto, el objeto no puede avanzar de `evidence_pending`. | Equipo de análisis (completar entrada en config/modules.yaml) | `evidence_pending` |
| OQ-OBJ-02 | Media | `safety_meetings.Update_External_Meeting_Participations` | Fuente SQL propuesta (`Asistentes_SM2025.sql`) es inferida por nombre, no confirmada. Sin clave de correlación documentada. `Reunionesdegrupo.zip` (Bloque2) reafirma la existencia del archivo pero no aporta una declaración explícita que lo vincule a este objeto — sigue siendo inferencia por nombre, no confirmación. | Bloquea `ready_for_draft` de este objeto específicamente (no afecta a Group Meetings). | Equipo de análisis | `pending_confirmation` |
| OQ-OBJ-03 | Alta | `eventos.Impacts` | La clave de correlación citada en config (`CS_HistImpactID`/`CS_HistImpactIDOH`, `config/modules.yaml:191`) no se ha podido re-verificar literalmente en la cabecera real leída de `Impacts-20072026-9.csv`. **Avance en el incremento de evidencia ETL**: el Index de los workbooks de Eventos Antiguos y Eventos Nuevos confirma que AMBOS campos destino se usan realmente, con claves origen distintas según la fase (`IdImpactoGenerado2↔CS_HistImpactIDOH`, `StandardPersonalImpactKey`/`IdImpactoGenerado↔CS_HistImpactID`) — resuelve que ambos existen, no resuelve si coexisten intencionalmente. | Una especificación que asuma solo una de las dos claves sin reverificar podría fallar según la fase del pipeline. | Equipo de análisis (confirmar con el cliente si la coexistencia de 2 claves por fase es intencional) | `pending_confirmation` — avanzada, no cerrada |
| OQ-OBJ-04 | Media | `eventos.Investigations` / `eventos.PSM_Forms` | Relación entre `Causes Data-22072026-73.csv` (189654 filas) y Eventos — **hipótesis actualizada en el incremento de análisis profundo ETL**: la hoja `CSV_PSM_ANS-2026-FULL` (workbook de Eventos Nuevos, datos de PSM) comparte las 2 primeras columnas exactas (`Causes`, `RowNo`) y el mismo patrón temático de "5 whys"/causas raíz — candidato más plausible que Investigations directamente, aunque el resto de nombres de columna no coincide exactamente (`CheckBox` vs `Answer Checkbox`, etc.). Sigue sin confirmación explícita. | Sin resolver, Causes Data queda como objeto huérfano sin módulo asignado — ahora con un candidato más fuerte (`eventos.PSM_Forms`) que antes (`eventos.Investigations`). | Equipo de análisis + cliente (contrastar cabecera completa de Causes Data contra CSV_PSM_ANS) | `pending_confirmation` — hipótesis refinada, no cerrada |
| OQ-OBJ-05 | Alta | `inspecciones.Inspection_Data` | El propio `config/modules.yaml:247` reconoce "export parcial, no se puede exportar completo" — no se documenta qué parte falta ni por qué (relacionado con el modelo polimórfico de respuesta). No se profundizó en el ETL de Inspecciones más allá de su Index en este incremento. | Bloquea `ready_for_draft` de este objeto — clasificado `blocked` en `export_readiness_matrix.md`. | Cliente (aclarar limitación de exportación de Enablon para este objeto) | `evidence_pending` |
| OQ-OBJ-06 | Media | `moc.Change_Register` (candidato) | Relación entre `Checklists Data-22072026-67.csv` (4381 filas) y los "Checklists de Risk Assessment" mencionados en `config/modules.yaml:143` — inferida solo por contexto, no confirmada. El Index de MOC confirma trabajo inconcluso en Risk Assessment ("estamos aún averiguando como generar los risk assesment") pero no menciona esta hoja de Checklists Data específicamente. | Sin resolver, Checklists Data queda como objeto huérfano sin módulo asignado. | Equipo de análisis + cliente | `pending_confirmation` |
| OQ-OBJ-07 | Media | `simulacros.List_of_Activities` | `Mapeo_Estado_AL` traduce el campo origen `Estado` a un vocabulario de destino (`Completed/In Progress/Not Started`) DISTINTO del usado por `Mapeo_Estado` para el mismo campo origen en Drills (`Validated/Pending validation/Draft`), con correspondencias distintas incluso para valores compartidos (`'Aprobado'` → `'Validated'` en Drills, → `'Not Started'` en List of Activities). No confirmado si es una decisión funcional deliberada (objetos Enablon distintos, vocabularios distintos) o un defecto de copia entre hojas. | Una especificación que asuma el mismo vocabulario para ambos objetos generaría un CSV con estados incorrectos para uno de los dos. | Cliente (confirmar vocabulario de estado correcto por objeto) | `evidence_pending` |

## Action Plans

| question_id | Prioridad | Objeto relacionado | Evidencia faltante | Impacto | Responsable sugerido | Estado |
|---|---|---|---|---|---|---|
| OQ-AP-01 | Alta | `ap.Action_Plans` | La mayoría de registros reales no tienen ningún campo de enlace poblado (`BCCrisis`, `CS_ByPasses`, etc.) — `config/modules.yaml:288-290` reconoce esto como pendiente de investigar, sin determinar si es esperado o defecto. | Afecta directamente qué proporción de Action Plans puede considerarse `linked_action_plan` con evidencia real vs. asumida. | Cliente + equipo de análisis | `evidence_pending` |
| OQ-AP-02 | Alta | `ap.Action_Plans` | 63% de los registros comparten `CS_HistoricalOriginID` con otro (11905/11906/11920 únicos de 31444 según metodología, ver `action_plan_relationship_analysis.md`) — sin determinar si es un patrón legítimo de agrupación (como Inspecciones→Observations) o un defecto de duplicación. **Cuantificado en este incremento**: 76.17% de las filas están en grupos compartidos con todos los `CS_HistoricalAPID` distintos (`legitimate_one_to_many_candidate`), 1.10% tienen `CS_HistoricalAPID` repetido dentro del mismo padre (`duplicate_action_candidate`). Confirma la hipótesis estructural (CS_HistoricalAPID separado de CS_HistoricalOriginID) con datos reales, no solo con la lógica del ETL. | Un `ExportDefinition` futuro necesita saber si debe deduplicar (el 1.10% conflictivo) o preservar el resto (76.17% legítimo). | Equipo de análisis (aplicar `is_legitimate_grouping()` de `src/analysis/duplicates.py` de forma ejecutable — hoy solo hay evaluación documental equivalente) | `pending_confirmation` — cuantificado, no cerrado (falta confirmación funcional explícita del cliente) |
| OQ-AP-03 | Media | `ap.Action_Plans` | **Resuelta**: `Bloque2_Mappings_SQL_ENA/AP.zip` se abrió en este incremento. Contiene únicamente un duplicado byte a byte de `sql/source_queries/AP/Acciones_correctoras.sql` — no un documento de mapping. El insumo más directo para mapear columnas reales de Action Plans sigue sin existir (los xlsx de Bloque3 son de entidad, no de campo). **Actualización**: los ETL de Bloque1 SÍ se abrieron en el incremento siguiente y confirmaron `CS_HistoricalAPID` como clave propia (ver `OQ-AP-02`). | Cualquier `ExportDefinition` de Action Plans se basaría solo en la cabecera del CSV real, no en un mapping documentado — confirmado, no solo asumido. | Equipo de análisis | `resolved` — respuesta negativa confirmada |
| OQ-AP-04 | Baja | `ap.Action_Plans` | El grupo (padre) con más acciones de todo el dataset tiene 179 acciones asociadas, todas con `CS_HistoricalAPID` distinto (cumple el criterio técnico de `legitimate_one_to_many_candidate`). No se ha contrastado con el origen si un volumen tan alto es plausible para un solo registro padre. | Un volumen atípico sin contrastar podría ocultar una agrupación incorrecta bajo apariencia de agrupación legítima. | Cliente (confirmar con el origen si 179 acciones para un mismo simulacro/evento/inspección es plausible) | `evidence_pending` |
| OQ-AP-05 | Media | `ap.Action_Plans` | 95 grupos (345 filas) tienen `CS_HistoricalAPID` repetido DENTRO del mismo padre — clasificados `duplicate_action_candidate`. Todos los ejemplos concretos localizados provienen de `GCT.CT_ACCIONES`, ninguno de `Prevención.ITP_ACCIONES_CORRECTORAS`, y al menos un caso muestra un patrón de repetición exactamente 2x (cada acción aparece exactamente dos veces) — sugiere causa sistemática, no error aislado de captura. | Sin resolver, cualquier deduplicación futura de Action Plans podría eliminar filas legítimas o preservar duplicados reales según se interprete mal este patrón. | Cliente + equipo de análisis (confirmar si el patrón 2x en GCT es un efecto conocido del proceso GCT, p. ej. creación+actualización contadas ambas) | `evidence_pending` |
| OQ-AP-06 | Alta | `ap.Action_Plans` | 1437 valores de `CS_HistoricalAPID` aparecen bajo AMBOS sistemas origen (`Prevención.ITP_ACCIONES_CORRECTORAS` y `GCT.CT_ACCIONES`) simultáneamente — colisión de identidad entre sistemas, hallazgo NUEVO y distinto de la ya conocida duplicidad de `CS_HistoricalOriginID`. No confirmado si Enablon usa su propio campo `Id` interno (no `CS_HistoricalAPID`) como clave real de unicidad, lo que haría esta colisión inocua para la carga ya realizada. | Si algún proceso futuro tratara `CS_HistoricalAPID` como identificador único global (sin considerar el sistema origen), podría fusionar o confundir 1437 pares de acciones no relacionadas. | Cliente (confirmar mecanismo real de unicidad en Enablon) | `evidence_pending` |

## Templates Enablon

| question_id | Prioridad | Objeto relacionado | Evidencia faltante | Impacto | Responsable sugerido | Estado |
|---|---|---|---|---|---|---|
| OQ-TPL-01 | Alta | Todos | No existe ningún template de importación en blanco confirmado por Enablon para ningún objeto — solo exportaciones de datos reales (Bloque4). No está probado que Enablon acepte en importación exactamente las mismas columnas/orden que expone en exportación. | Bloquea que cualquier objeto alcance `template_status: validated` — todos quedan en `candidate` como máximo (ver `export_readiness_matrix.md`). | Cliente (solicitar/confirmar template de importación oficial por objeto Enablon) | `evidence_pending` |
| OQ-TPL-02 | Media | `bypass.By_Passes` | Confirmado que ciertos campos (`GOS`, `FechaEnvioFase1..6`, etc.) NO son importables vía CSV/API — pendiente confirmar si esta lista es exhaustiva o solo lo detectado hasta ahora. | Un `ExportDefinition` que no excluya estos campos generaría un CSV que Enablon rechazaría o ignoraría silenciosamente. | Cliente | `partially_reviewed` |
| OQ-TPL-03 | Media | `moc.Change_Register` | `config/modules.yaml:143` menciona un bloqueo "no automatizable sin API Enablon" para Checklists de Risk Assessment (requiere generar formularios vacíos en la UI y reimportar). No está documentado si esto aplica solo a Checklists o a Change Register completo. | Afecta el alcance realista de cualquier `ExportPlan` para MOC. | Cliente | `evidence_pending` |

## Formato CSV

| question_id | Prioridad | Objeto relacionado | Evidencia faltante | Impacto | Responsable sugerido | Estado |
|---|---|---|---|---|---|---|
| OQ-CSV-01 | Alta | Todos | Los 17 CSV de Bloque4 usan dos convenciones distintas: mayoría UTF-16LE tab-delimited, un subconjunto (Causes Data, Checklists Data, Inspection Data, PSM forms, Update External Meeting Participations) ISO-8859-1/ASCII semicolon-delimited. Sin confirmar por qué difieren ni cuál es la convención que Enablon exige en importación. | Un `CSV Writer` futuro necesita saber si el encoding/delimitador depende del objeto, de la fecha de export, o de otra causa — hoy es solo una observación, no una regla. | Cliente (o volver a exportar todos los objetos con el mismo método para descartar causa) | `pending_confirmation` |
| OQ-CSV-02 | Media | Todos | `config/settings.yaml` fija `default_encoding: "utf-8-sig"` para los CSV que genera este framework — no confirmado si Enablon acepta ese encoding en importación o si exige UTF-16LE como en sus propios exports. | Afecta directamente el diseño del futuro `CSV Writer`. | Cliente | `evidence_pending` |
| OQ-CSV-03 | Baja | Todos | Orden de columnas: no está confirmado si Enablon exige un orden específico en importación o si lo resuelve por nombre de cabecera. | Afecta si `ExportDefinition` necesita fijar orden exacto o solo nombres de columna correctos. | Cliente | `evidence_pending` |

## Configuración por cliente

| question_id | Prioridad | Objeto relacionado | Evidencia faltante | Impacto | Responsable sugerido | Estado |
|---|---|---|---|---|---|---|
| OQ-CLIENT-01 | Alta | `bypass.By_Passes` | Confirmado que la carga requiere "desactivar 'fieldsets' de validación en Enablon antes de cargar y restaurarlos después" (`config/modules.yaml:166`) — no documentado quién tiene permisos para hacerlo ni el procedimiento exacto. | Bloquea el diseño de `ExportPackage`/proceso de entrega para este objeto si no se coordina con el cliente. | Cliente (administrador Enablon) | `evidence_pending` |
| OQ-CLIENT-02 | Media | Todos | No se ha solicitado formalmente acceso a la API de Enablon (`CLAUDE.md`, tabla "Estado de los accesos") — `.env.example` ya reserva `ENABLON_BASE_URL`/`ENABLON_API_TOKEN`, pero sin uso real todavía. | Determina si el Export Engine futuro podrá validar contra un catálogo de campos en vivo o solo contra templates estáticos. | Cliente + equipo de proyecto | `pending_confirmation` |
| OQ-CLIENT-03 | Baja | `moc.Change_Register` | Confirmar con el cliente si el patrón de exposición al Hallazgo #1 (La Rábida/Palos) tiene una causa administrativa conocida (permisos, scope de carga por site) antes de asumir que es un defecto técnico del ETL. | Determina si la corrección es de scope de query o de proceso administrativo del cliente. | Cliente | `pending_confirmation` |

## ETL y trazabilidad (nuevo — incremento de evidencia ETL)

| question_id | Prioridad | Objeto relacionado | Evidencia faltante | Impacto | Responsable sugerido | Estado |
|---|---|---|---|---|---|---|
| OQ-ETL-01 | Media | `simulacros.Drills` | La regla `Asis_concat`, referenciada como `Transformation From` para `CS_HistoricalDrillAttendees`, no muestra su lógica de concatenación de múltiples asistentes en la muestra de filas leída (2-6). | Sin esto, no se puede documentar cómo se combinan varios asistentes en un solo campo de texto. | Equipo de análisis (releer la hoja completa, no solo la muestra) | `evidence_pending` |
| OQ-ETL-02 | Media | `simulacros.Drills` | El ETL calcula 4 componentes de duración (`CS_DurationMonths/Days/Hours/Minutes`) en `DB_OrigenSim`, pero el CSV real de Drills tiene una única columna `CS_Duration`. No se ha localizado la fórmula que recombina los 4 componentes en el valor único. | Una especificación futura de este campo no puede fijar la regla de conversión sin esta pieza. | Equipo de análisis (buscar en columnas/hojas no leídas en este incremento) | `evidence_pending` |
| OQ-ETL-03 | Alta | `simulacros.Drills` | ~~El campo `Reference` tiene 3 fórmulas distintas confirmadas entre `CSV_SIM_full`, `CSV_SIM` y `CSV_Generated_BCM_SIM` (una de ellas sin el literal "HIST"). No se puede determinar cuál produjo el dato realmente cargado en Enablon.~~ **Descripción y evidencia original conservadas sin cambios — ver histórico.** Impacto original: bloqueaba fijar con confianza la regla de `Reference`, por lo que `simulacros.Drills` no avanzaba a `object_specification_draft_ready`. | (impacto original conservado arriba) | Cliente — **resuelto**: el consultor responsable de la migración aprobó y comunicó la regla funcional vigente. | `resolved` — `resolution_type: approved_functional_decision`, `resolution_evidence_id: AFD-DRILLS-REFERENCE-001`, `resolved_rule: "<CS_Typology>-HIST-<CS_HistoricalOriginID>-<StartingDate(dd/MM/yyyy)>"`, `resolution_date: 2026-07-23`. Las 2 variantes de fórmula que SÍ reproducen la regla (`CSV_SIM_full`, `CSV_SIM`) se confirman `current_rule`; la variante de `CSV_Generated_BCM_SIM` se confirma `residual_rule_candidate` (descartada, no reproduce la regla). Ver `decision_packages/drills_reference_decision_package.md` y `evidence/drills_reference_variant_catalog.yaml`. **No reabierta.** |
| OQ-ETL-04 | Alta | `simulacros.Drills` | ~~`MapeoSims` declara `IDCentro` sin destino de campo — la resolución de entidad no ocurre dentro de esta hoja de mapeo. No se ha identificado qué proceso la resuelve.~~ Descripción e impacto original conservados. | (impacto original conservado arriba) | Equipo de análisis — **resuelto**: localizado en `DB_OrigenSim` (columnas `RutaEnablon`/`Nombre Entidad`), no en `MapeoSims`. | `resolved` — la resolución de entidad usa `IDUnidadOrg` (no `IDCentro`) vía 2 XLOOKUP contra la tabla `Entidades_Enablon_ITP` (= catálogo antiguo `Entidades_Mapeo`, 681 filas), ya confirmada como decisión aceptada para Simulacros en `config/modules.yaml:84`. Ver `object_assessments/drills_entity_resolution_assessment.md`. `trace_status` de este campo pasa a `fully_traced`. |
| OQ-ETL-05 | Media | `simulacros.Drills` | ¿Por qué existen 3 hojas de salida distintas para el mismo campo `Reference` (`CSV_SIM_full`, `CSV_SIM`, `CSV_Generated_BCM_SIM`), y en qué orden/momento se generó cada una? La variante `CSV_Generated_BCM_SIM` no reproduce la regla aprobada — no se sabe si es una versión anterior sustituida o un intento fallido. | No bloquea la decisión ya registrada (`AFD-DRILLS-REFERENCE-001`), pero deja sin explicar por qué la variante descartada existe en el mismo workbook. | Cliente / equipo de análisis | `evidence_pending` |
| OQ-ETL-06 | Media | `simulacros.Drills` | Tratamiento de `Reference` cuando `CS_Typology`, `CS_HistoricalOriginID` o `StartingDate` están vacíos — no confirmado por el consultor. La muestra leída (14 filas) no contenía ningún componente vacío. | Sin esto, una especificación no puede fijar el comportamiento de `Reference` para registros con datos incompletos. | Cliente (consultor responsable de la migración) | `evidence_pending` — pregunta nueva, no reabre `OQ-ETL-03` |

## Mapeo de entidad (nuevo — incremento de evidencia de entidad/Bloque3)

| question_id | Prioridad | Objeto relacionado | Evidencia faltante | Impacto | Responsable sugerido | Estado |
|---|---|---|---|---|---|---|
| OQ-ENT-01 | Baja | `moc.Change_Register` | La hoja `Niveles` del workbook GCT (UORG) devuelve `'NULL.NULL'` en el código final para varias filas de muestra tras encadenar `XLOOKUP` entre 4 hojas — no confirmado si es un resultado esperado (unidad sin subdivisión) o una cadena de lookup rota. | Si es un lookup roto, cualquier resolución de entidad de MOC basada en esta hoja heredaría el error silenciosamente. | Equipo de análisis (muestrear más filas y confirmar el patrón) | `evidence_pending` |
| OQ-ENT-02 | Media | Todos los módulos ITP | No se ha confirmado cuál de las dos hojas candidatas a "mapeo final" del workbook `Mapeo ITP primer eje...` (`Mapeo Eje final-modificado`, 1696 filas, o `Eje final 2`, 3038 filas, con 5 idiomas completos) es la efectivamente usada para producir `inputs/entity_catalog/catalogo_resuelto_code_ruta_site.csv`. | Afecta qué fuente citar como autoritativa para la resolución de entidad en cualquier especificación futura de un objeto ITP. | Equipo de análisis + cliente | `pending_confirmation` |
| OQ-ENT-03 | Baja | Ninguno específico (organizativo) | Varias hojas por site (`Site Canarias`, `MCPM`) contienen instrucciones en lenguaje natural de reestructuración organizativa pendientes de ejecutar (p. ej. "dividir dos centros L4"), no decisiones ya aplicadas. No se ha confirmado si estas reestructuraciones ya se ejecutaron en el árbol de entidades vigente (`First_Axis_export_bruto.csv`) o siguen pendientes. | Si siguen pendientes, el catálogo de entidades vigente podría no reflejar la estructura organizativa real esperada por el cliente para estos sites. | Cliente | `pending_confirmation` |
| OQ-ENT-04 | Media | `bypass.By_Passes`, `eventos.Events` (antiguos), `ops.JSO`, `safety_meetings.Group_Meetings` | Confirmado en el incremento de análisis profundo ETL: estos 4 objetos usan el MISMO mecanismo de resolución de entidad que `simulacros.Drills` (2 XLOOKUP contra la tabla `Entidades_Enablon_ITP`, 681 filas — el catálogo antiguo deprecado). Para `simulacros` esto está explícitamente aceptado como correcto para su época (`config/modules.yaml:84`). **No existe una nota equivalente para estos otros 4 objetos.** | Sin confirmar, no se sabe si depender del catálogo deprecado es igualmente aceptable para estos objetos o si explica parte de su gap de volumetría no resuelto (recordar que `bypass`, `ops` y `safety_meetings` siguen en `hallazgo_1_infra_migracion.modulos_pendientes_de_revisar`, `config/modules.yaml:71`). | Cliente + equipo de análisis | `evidence_pending` |
